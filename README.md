# rdp-mcp

`rdp-mcp` is a Rust Model Context Protocol server for controlling Remote Desktop sessions.

The project is based on the RDP helper from
[conduit-desktop](https://github.com/advenimus/conduit-desktop) and uses
[FreeRDP](https://www.freerdp.com/) for the native RDP implementation.
